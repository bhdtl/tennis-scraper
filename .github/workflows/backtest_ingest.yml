name: 🏺 Historical Data Ingest (V4)

on:
  workflow_dispatch: # Manueller Start über GitHub

jobs:
  ingest-history:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Install Dependencies
        run: |
          pip install pandas requests supabase openpyxl python-dotenv

      - name: 🚀 Run Ingestion Script
        # DAS HIER IST DER FEHLENDE SCHLÜSSEL ZUM ERFOLG:
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          # WICHTIG: Wir mappen den Service Key auf beide Variablen zur Sicherheit
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python backtest_ingest.py
